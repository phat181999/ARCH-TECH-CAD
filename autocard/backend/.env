DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tanphat99
DB_NAME=arch-cad
DB_SSLMODE=disable
SERVER_PORT=8080
JWT_SECRET=autocard-dev-secret-change-in-production

# ── AI (server-side only — NEVER put this in the frontend) ───────────────────
# You can use OPENAI_API_KEY or GEMINI_API_KEY.
